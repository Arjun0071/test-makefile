apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "demoapp-chart.fullname" . }}-test-connection"
  labels:
    {{- include "demoapp-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "demoapp-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
